namespace EliteCommodityAnalysis.Repository.Managers {
    // Handles aggregating data from a fresh batch of API dumps or a new API transaction and aggregates it into the
    // persistent data structures from which we source our UI
    public class AggregateManager {

    }
}